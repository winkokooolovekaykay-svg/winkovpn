<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="180dp"
    android:background="@drawable/bg_nav_header_gradient"
    android:gravity="bottom"
    android:orientation="vertical"
    android:padding="20dp"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <!-- App Logo / Shield Icon -->
    <FrameLayout
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginBottom="12dp">

        <ImageView
            android:id="@+id/imgNavLogo"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_icon_circle_glow"
            android:contentDescription="@string/app_name"
            android:padding="10dp"
            android:src="@drawable/ic_vpn_key_round"
            app:tint="@color/primary" />

        <!-- Green Online Status Dot Indicator -->
        <View
            android:layout_width="12dp"
            android:layout_height="12dp"
            android:layout_gravity="bottom|end"
            android:background="@drawable/bg_status_indicator_green" />
    </FrameLayout>

    <!-- App Name Title -->
    <TextView
        android:id="@+id/tvNavTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textAppearance="@style/TextAppearance.AppCompat.Headline"
        android:textColor="@color/text_primary"
        android:textSize="20sp"
        android:textStyle="bold" />

    <!-- Developer Credit Subtitle: Developed by WinKoKoOo -->
    <TextView
        android:id="@+id/tvNavSubtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:text="@string/developer_name"
        android:textColor="@color/text_developer_highlight"
        android:textSize="13sp"
        android:textStyle="italic|bold" />

    <!-- Version Badge -->
    <TextView
        android:id="@+id/tvNavVersion"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:background="@drawable/bg_version_pill"
        android:paddingHorizontal="8dp"
        android:paddingVertical="2dp"
        android:text="@string/app_version"
        android:textColor="@color/text_secondary"
        android:textSize="11sp" />

</LinearLayout>
